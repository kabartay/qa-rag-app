version: '3.8'

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  rag-simple:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    depends_on:
      - redis
    command: make run-simple

volumes:
  redis_data:
